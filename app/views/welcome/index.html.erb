<!-- marketing banner -->
<%= render 'layouts/marketing_banner' %>

<!-- navigation section -->
<%= render 'layouts/navbar' %>

<!-- big picture section -->
<%= render 'big_pic' %>

<!-- how it works section -->
<%= render 'how_it_works' %>

<%= render 'quality_banner' %>

<!-- customers section -->
<%= render 'customers' %>

<!-- get started section -->
<%= render 'last_banner' %>


<!-- copyright section -->
<%= render 'layouts/footer' %>

<%= render 'index_js' %>

<!-- Notes -->
<%= render 'notes' %>