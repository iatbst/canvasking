<div class="col-md-2">
		<div class="box" align="center" style="height:400px; width:300px; margin-left:20px; position:fixed">

			<h3 style="margin-top: 40px">Your Cart</h3>
			<hr style="width: 80% ; border-width: 1px; border-color:#E2E2E2;"/>
			
			<h4 >
				<b id="cart_quantity" class="no_bold"><%= cart.quantity %></b>
				<b id="item_single_or_plural" class="no_bold"> 
				<% if cart.quantity > 1 %>
					<%= "item".pluralize %>
				<% else %>
					<%= "item" %>
				<% end %>
				</b>
				: $<b id="cart_price"><%= cart.price %></b>
			</h4>
			
	
			<br />
			<%= button_to 'CHECKOUT', new_order_path, method: :get, class:"btn btn-primary btn-lg", style:"width: 80%" %>
				<br />
			
			<%= button_to 'CONTINUE SHOPPING', new_item_path, method: :get, class:"btn btn-secondary btn-lg", style:"width: 80%" %>

			
				<br />
				<div style="width: 85%; margin-left: 10px">
					<p style="font-size: 12px; text-align: left">
						<em> Does not include tax or shipping. Chat with us or email support@canvasking.com with any questions about your order. </em>
					</p>
				</div>
			
		</div>
	</div>